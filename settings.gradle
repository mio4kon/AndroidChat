include ':app'
println "--------1 -------"


def sout = new StringBuilder(), serr = new StringBuilder()
def proc = 'cat /ws/mars_test/ilint_mars/readme.md'.execute()
proc.consumeProcessOutput(sout, serr)
proc.waitForOrKill(1000)
println"out> $sout"
